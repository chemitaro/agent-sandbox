# macOS
.DS_Store

# Local agent settings (git-ignored, keep placeholder tracked)
.agent-home/*
!.agent-home/.keep
.env

# User-managed tools (git-ignored, keep this doc tracked)
tools/*
!tools/README.md
